import 'package:flutter/material.dart';

class SectionHeads extends StatelessWidget {
  const SectionHeads({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: CircularProgressIndicator(),),
    );
  }
}
